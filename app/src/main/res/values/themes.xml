<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieTicketOrder" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MovieTicketOrder" parent="Base.Theme.MovieTicketOrder" />
    
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_splash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/profil_company</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.MovieTicketOrder</item>

    </style>
</resources>